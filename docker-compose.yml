version: '3'
services:
  #MongoDB
  mongodb:
    image: mongo
    volumes:
      - ./mongodb:/data/db

  app:
    build: .
    ports:
      - 3000:3000
    env_file:
      - .env
    depends_on:
      - mongodb

volumes:
  mongodb: {}
